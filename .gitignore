.DS_Store
.env
.env.local
.idea
.vscode/
tmp/
bin/
dist/
*.log
coverage/
*.test
# Local machine-specific compose overrides (e.g. port remaps to avoid
# colliding with another project already running on this host) -- never
# meant to be shared, see docker-compose.override.yml's own comment.
docker-compose.override.yml
